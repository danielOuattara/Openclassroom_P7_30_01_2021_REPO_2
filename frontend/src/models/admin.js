
export default class Admin {
    constructor(email, password, special, roles ) {
        this.email = email;
        this.password = password;
        this.special=special;
        this.roles = roles
    }
}